import 'package:flutter/material.dart';

class FavoturiteScreen extends StatelessWidget {
  const FavoturiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favoturite Screen Page'));
  }
}